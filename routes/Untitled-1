const findUser(login, password) async => {
    usersData.findOne({
        where: {
            login,
            // password
        }
    });

{




const user = await findUser(req.body.login, req.body.password)
const tmpUser = await cosTamUser(user)