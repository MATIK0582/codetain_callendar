<section id="adminLeave" class="container">
    <div class="gig">
        <p>admin</p>
        <h1>{{day}}
            {{month}}
            {{year}}
            {{description}}
            {{leaveType}}
            {{status}}
        </h1>
    </div>
</section>